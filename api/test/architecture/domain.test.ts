describe('architecture', () => {
  it('business logic should not depend on the ui', async () => {
    // TODO (Valle) -> add tests, maybe using an abstract syntax tree?
    expect(true).toBe(true);
  });
});
